name: Trigger auto deployment for container-app-nofegvmokrpmy

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-nofegvmokrpmy-AutoDeployTrigger-7344d5aa-9010-413f-afcc-679149d9606e.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPNOFEGVMOKRPMY_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPNOFEGVMOKRPMY_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPNOFEGVMOKRPMY_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPNOFEGVMOKRPMY_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPNOFEGVMOKRPMY_REGISTRY_PASSWORD }}
          containerAppName: container-app-nofegvmokrpmy
          resourceGroup: appspacesragapp83928c86
          imageToBuild: default/container-app-nofegvmokrpmy:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


